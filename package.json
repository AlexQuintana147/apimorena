{
  "name": "proyecto-hola-mundo",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "build": "chmod +x build.sh && ./build.sh && npm install"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  },
  "dependencies": {
    "express": "^4.21.1"
  }
}
